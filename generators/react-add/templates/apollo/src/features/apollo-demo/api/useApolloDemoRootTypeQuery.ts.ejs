import type { QueryResult } from '@apollo/client/react';
import { useQuery } from '@apollo/client/react';
import {
  APOLLO_DEMO_ROOT_TYPE_QUERY,
  apolloDemoRootTypeDataSchema,
  type ApolloDemoRootTypeData,
} from '../model';

export function useApolloDemoRootTypeQuery(): QueryResult<ApolloDemoRootTypeData> {
  const result = useQuery<ApolloDemoRootTypeData>(APOLLO_DEMO_ROOT_TYPE_QUERY, {
    fetchPolicy: 'cache-and-network',
    notifyOnNetworkStatusChange: true,
  });

  return {
    ...result,
    data: result.data ? apolloDemoRootTypeDataSchema.parse(result.data) : undefined,
  };
}
