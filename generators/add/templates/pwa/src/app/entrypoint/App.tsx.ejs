import { BrowserRouter } from 'react-router-dom';
import { AppProviders } from '@/app/providers';
import { AppRouter } from '@/app/routes';
import { PwaProvider, PwaStatus } from '@/features/pwa';
import '@/app/styles/global.css';

export function App() {
  return (
    <BrowserRouter>
      <PwaProvider>
        <AppProviders>
          <PwaStatus />
          <AppRouter />
        </AppProviders>
      </PwaProvider>
    </BrowserRouter>
  );
}
